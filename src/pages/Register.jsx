import React,{useState}from"react";import{Link,useNavigate}from"react-router-dom";import{useAuth}from"../context/AuthContext";
export default function Register(){const{register}=useAuth();const nav=useNavigate();const[name,setName]=useState(""),[email,setEmail]=useState(""),[password,setPassword]=useState(""),[error,setError]=useState(""),[busy,setBusy]=useState(false);const submit=async e=>{e.preventDefault();setError("");setBusy(true);try{await register(name,email,password);nav("/account")}catch(x){setError(x.message)}finally{setBusy(false)}};return <section className="section"><div className="container auth-card"><span className="eyebrow">SIDRA FABRICS ACCOUNT</span><h1 className="section-title">Create account.</h1><p className="muted">Save your wishlist, track orders and check out faster.</p><form onSubmit={submit} className="auth-form"><input required minLength={2} placeholder="Full name" value={name} onChange={e=>setName(e.target.value)}/><input required type="email" placeholder="Email address" value={email} onChange={e=>setEmail(e.target.value)}/><input required minLength={8} type="password" placeholder="Password (8+ characters)" value={password} onChange={e=>setPassword(e.target.value)}/>{error&&<div className="alert">{error}</div>}<button className="btn btn-dark" disabled={busy}>{busy?"CREATING…":"CREATE ACCOUNT"}</button></form><p className="auth-switch">Already have an account? <Link to="/login">Sign in</Link></p></div></section>}
