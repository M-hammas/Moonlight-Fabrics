import React from "react";import ProductGrid from "../components/shop/ProductGrid";import {useProducts} from "../context/ProductContext";export default function NewArrivals(){const{products}=useProducts();const items=products.filter(p=>p.isNew??p.is_new);return <section className="section"><div className="container"><div className="page-hero"><span className="eyebrow">SIDRA FABRICS COLLECTION</span><h1>New Arrivals</h1><p>Fresh pieces selected for modern everyday living.</p></div><ProductGrid products={items} loading={false}/></div></section>}