import React from "react";import ProductGrid from "../components/shop/ProductGrid";import {useProducts} from "../context/ProductContext";export default function Sale(){const{products}=useProducts();const items=products.filter(p=>p.isSale??p.is_sale);return <section className="section"><div className="container"><div className="page-hero"><span className="eyebrow">SIDRA FABRICS COLLECTION</span><h1>The Sale Edit</h1><p>Signature styles, special prices and limited-time offers.</p></div><ProductGrid products={items} loading={false}/></div></section>}