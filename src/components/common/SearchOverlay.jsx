import React,{useEffect,useState} from 'react';import {Link,useNavigate} from 'react-router-dom';import api from '../../services/api';
export default function SearchOverlay({open,onClose}){const [q,setQ]=useState('');const [items,setItems]=useState([]);const nav=useNavigate();useEffect(()=>{if(!open)return;const t=setTimeout(()=>{if(q.trim())api.get(`/products?search=${encodeURIComponent(q)}`).then(setItems).catch(()=>setItems([]));else setItems([])},180);return()=>clearTimeout(t)},[q,open]);if(!open)return null;return <div className="search-overlay" role="dialog" aria-modal="true"><button className="overlay-close" onClick={onClose}>×</button><div className="search-modal"><span className="eyebrow">SEARCH SIDRA FABRICS</span><form onSubmit={e=>{e.preventDefault();onClose();nav(`/shop?q=${encodeURIComponent(q)}`)}}><input autoFocus value={q} onChange={e=>setQ(e.target.value)} placeholder="Search clothing, fragrance, shoes…"/><button>Search</button></form><div className="search-results">{items.map(p=><Link key={p.id} to={`/product/${p.id}`} onClick={onClose}><span>{p.name}</span><small>PKR {Number(p.sale_price??p.price).toLocaleString()}</small></Link>)}</div></div></div>}
