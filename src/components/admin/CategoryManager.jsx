import React,{useState} from "react";import {CATEGORIES} from "../../utils/constants";
export default function CategoryManager(){const [cats,setCats]=useState(CATEGORIES);const [name,setName]=useState("");return <div className="category-manager"><div className="inline-form"><input value={name} onChange={e=>setName(e.target.value)} placeholder="New category"/><button className="btn btn-dark" onClick={()=>{if(name.trim()){setCats(x=>[...x,{key:name.toLowerCase().replace(/\s+/g,"-"),label:name}]);setName("")}}}>Add</button></div><div className="category-list">{cats.map(c=><div key={c.key}><span>{c.label}</span><button onClick={()=>setCats(x=>x.filter(a=>a.key!==c.key))}>×</button></div>)}</div></div>}
